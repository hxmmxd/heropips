<<<<<<< HEAD
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.metaapi/
/doc_temp

=======
node_modules/
.next/
dist/
.turbo/
coverage/
.DS_Store
*.tsbuildinfo

# Alternate Next build dirs (NEXT_DIST_DIR=.next-audit/.next-verify/...)
.next-*/

# ---- secrets ----------------------------------------------------------------
# Every environment file is ignored; only the *.example templates are tracked.
# `.env` and `*.local` alone left .env.staging and .env.production exposed.
.env
.env.*
!.env.example
!.env.*.example
*.local

# Local operational artifacts
/backups/
infra/compose/backups/

# Lighthouse / QA report output (scripts/lighthouse.mjs --json). CI uploads
# these as build artifacts; they are never committed — 41 MB per full sweep.
/.audit/
>>>>>>> b44d13a (feat: initialize HeroPips repository with local docker stack fixes)
